<div class="  hero min-h-screen bg-base-200">
	<div class="hero-content text-center">
		<div class="max-w-md">
			<h1 class="text-5xl font-bold">Gestion de données du service de missions</h1>
			<br>


			<div class="flex flex-wrap justify-center">
				<a href="{{path('missions')}}"><img class="w-max" src="{{ asset('images/logobdd.png')}}" alt="..."/></a>
			</div> 

			{% if app.user %}
        <div class="">
            You are logged in as {{ app.user.nom }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% endif %}

			<a href="{{path('app_login')}}"><button  class="btn btn-active btn-ghost" >Login</button> </a>
		</div>
	</div>
</div>
